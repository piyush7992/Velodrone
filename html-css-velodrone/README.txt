This archive contains the standalone HTML pages and a combined CSS file for quick offline testing.
Files:
- index.html
- products.html
- contact.html
- survey-drone.html
- surveillance-drone.html
- counter-drone.html
- static.css (styles)

To preview locally, open any HTML file in a browser. The CSS is referenced as /styles/static.css in the HTML; adjust the paths or run a simple static server that serves the archive root as the site root.
